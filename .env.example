POSTGRES_USER=waypoint
POSTGRES_PASSWORD=waypoint
POSTGRES_DB=waypoint

# Modelos. Haiku arma itinerarios, Sonnet conversa.
ANTHROPIC_API_KEY=
PLANNER_MODEL=claude-haiku-4-5-20251001
CHAT_MODEL=claude-sonnet-5

# OpenRouteService: cuenta gratis en account.heigit.org, sin tarjeta.
ORS_API_KEY=
# Segundos minimos entre llamadas a ORS. La leccion de Voyage: ante un limite
# conocido se controla el ritmo en origen, no se reintenta a ciegas.
ORS_MIN_INTERVAL_SECONDS=1.5
# Techo propio de peticiones por ventana de 24 horas. El endpoint de matriz da
# 50 y el espaciado de arriba no lo evita: cincuenta llamadas a segundo y medio
# agotan el cupo en poco mas de un minuto. Al llegar aca, los traslados se
# estiman en vez de fallar.
ORS_DAILY_BUDGET=450
# Cupos del plan Standard de ORS: 500/dia la matriz, 2000/dia las direcciones.
# Estos son techos propios con 10% de margen.
ORS_DIRECTIONS_BUDGET=1800

# Origenes permitidos para CORS, separados por coma.
CORS_ORIGINS=http://localhost:3000

# Limite por IP. La demo no tiene cuentas y cada itinerario gasta tokens de
# Haiku mas, en zona fria, una peticion del cupo diario de rutas.
PLAN_PER_MINUTE=4
PLAN_PER_DAY=40
ITINERARY_PER_MINUTE=12
ITINERARY_PER_DAY=120
# Poner en true SOLO detras de un proxy que reescriba X-Forwarded-For, como
# Railway. En local, confiar en ese encabezado es regalar el limite: lo manda
# quien quiera y basta cambiarlo en cada peticion para saltarselo.
TRUST_PROXY_HEADER=false
